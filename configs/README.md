# Config mirrors

Keep only non-secret configuration examples or sanitized mirrors here.

Do not store `.env`, credentials, database dumps, API keys, tokens, or server
private configuration in this directory.

